{
  "extends": ["next/core-web-vitals", "plugin:prettier/recommended"],
  "plugins": ["prettier"],
  "rules": {
    "prettier/prettier": ["error"],
    "max-lines": ["warn", { "max": 300, "skipBlankLines": true, "skipComments": true }],
    "max-lines-per-function": [
      "warn",
      { "max": 100, "skipBlankLines": true, "skipComments": true }
    ],
    "no-restricted-imports": [
      "error",
      {
        "paths": [
          {
            "name": "@/app/_lib/api",
            "message": "Use React Query hooks and BFF routes instead of direct API calls"
          },
          {
            "name": "@/lib/api",
            "message": "Use React Query hooks and BFF routes instead of direct API calls"
          }
        ]
      }
    ]
  },

  "overrides": [
    {
      "files": ["**/*.{ts,tsx}"],
      "excludedFiles": ["app/_schemas/**"],
      "rules": {
        "no-restricted-imports": [
          "error",
          {
            "name": "zod",
            "message": "📐 Zod スキーマは app/_schemas/** に定義してください（route や hooks では import のみ可）。"
          }
        ]
      }
    }
  ]
}
